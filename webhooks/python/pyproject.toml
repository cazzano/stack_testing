[project]
name = "webhook-project"
version = "0.1.0"
description = "True Webhook — PostgreSQL LISTEN/NOTIFY + asyncpg"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy>=2.0.0",
    "asyncpg>=0.29.0",
    "httpx>=0.27.0",
    "psycopg2>=2.9.11",
]
